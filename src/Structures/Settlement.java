package Structures;

import Logic.IntersectionPosition;

import java.awt.*;

public class Settlement extends Structure {
    public Settlement(String type, String color, IntersectionPosition pos) {
        super(type, color, pos);
    }
}
